import { styled } from '..'

export const HomeContainer = styled('main', {
  position: 'relative',

  display: 'flex',

  width: '100%',
  maxWidth: 'calc(100vw - ((100vw - 73.75rem) / 2))',
  minHeight: 656,

  marginLeft: 'auto',
})
